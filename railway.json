{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx --yes pnpm@9.15.9 install --no-frozen-lockfile && cd apps/api && npx prisma generate && npx nest build && echo '=== DIST ===' && ls -la dist/ && echo '=== MAIN CHECK ===' && ls dist/main.js && echo 'main.js OK'"
  },
  "deploy": {
    "startCommand": "echo '=== STARTING ===' && ls -la /app/apps/api/dist/ && node apps/api/dist/main",
    "healthcheckPath": "/api/v1/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
